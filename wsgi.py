from app import app as app  # if your Flask instance is named `app` inside app.py

# If you use an app factory pattern (create_app), uncomment the lines below and delete the line above:
# from app import create_app
# app = create_app()
